-- Use this initialize script to make any changes to your restored
--   test database before you upgrade it.
--
-- This initialize script currently only returns the date.
-- It could be setup to drop custom tables or views in your database
-- that conflick with the upgrade.
SELECT CURRENT_DATE;